node_modules/
*/node_modules/

# build output
server/dist/
client/dist/

# runtime session persistence
server/data/*
# ...but ship the synced player pool so deployments have real data
!server/data/players-yahoo.json

# secrets / env (keep .env.example tracked)
.env
.env.*
!.env.example

# logs / misc
*.log
.DS_Store
.vite/

# ts incremental build info
*.tsbuildinfo
